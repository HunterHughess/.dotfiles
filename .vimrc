Hunter Hughes. Vim commands were used to create the file and edit it after saving and 
exiting. The orginal file was named vimrc but later the cd command was used to create a copy of the file while also renaming it. The only issue encountered was forgetting how to save and quit a vim (use esc, :wq). 
syntax on
set ruler
set autoindent
set number 
set ignorecase 
color elflord
